/**
 * 4)	Haz javascript para : Calcular el perímetro de un cuadrado. 
 * Los valores deben de ser capturados con promt y mostrados con alertas.
 */

let lado = prompt('Dame el lado de el cuadrado');

let resultado = lado * 4;

alert('El perimetro del cuadrado es: ' + resultado);